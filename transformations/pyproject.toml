[project]
name = "weather-pipeline-dbt"
version = "1.0.0"
description = "dbt transformation layer — DuckDB (read S3 Parquet) + PostgreSQL (ATTACH for core/mart)"
requires-python = ">=3.12"

dependencies = [
    "dbt-duckdb>=1.9.0",
    "dbt-postgres>=1.9.0",
    "elementary-data[postgres]",
    "minio>=7.2.0",
    "openlineage-dbt>=1.0.0",
]
